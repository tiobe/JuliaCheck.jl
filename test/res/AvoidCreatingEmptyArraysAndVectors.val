>> Processing file 'AvoidCreatingEmptyArraysAndVectors.jl'...

AvoidCreatingEmptyArraysAndVectors.jl(43, 1):
resize!(w_pre_allocate, index-1)
└──────────────────────────────┘ ── Avoid resizing arrays after initialization.
Avoid resizing arrays after initialization.
Rule: avoid-creating-empty-arrays-and-vectors. Severity: 8

AvoidCreatingEmptyArraysAndVectors.jl(49, 1):
push!(w, 1)
└─────────┘ ── Avoid resizing arrays after initialization.
Avoid resizing arrays after initialization.
Rule: avoid-creating-empty-arrays-and-vectors. Severity: 8

AvoidCreatingEmptyArraysAndVectors.jl(50, 1):
pushfirst!(w, 1)
└──────────────┘ ── Avoid resizing arrays after initialization.
Avoid resizing arrays after initialization.
Rule: avoid-creating-empty-arrays-and-vectors. Severity: 8

AvoidCreatingEmptyArraysAndVectors.jl(51, 1):
pop!(w)
└─────┘ ── Avoid resizing arrays after initialization.
Avoid resizing arrays after initialization.
Rule: avoid-creating-empty-arrays-and-vectors. Severity: 8

AvoidCreatingEmptyArraysAndVectors.jl(52, 1):
popfirst!(w)
└──────────┘ ── Avoid resizing arrays after initialization.
Avoid resizing arrays after initialization.
Rule: avoid-creating-empty-arrays-and-vectors. Severity: 8

AvoidCreatingEmptyArraysAndVectors.jl(53, 1):
append!(w, [2, 3])
└────────────────┘ ── Avoid resizing arrays after initialization.
Avoid resizing arrays after initialization.
Rule: avoid-creating-empty-arrays-and-vectors. Severity: 8

AvoidCreatingEmptyArraysAndVectors.jl(54, 1):
prepend!(w, [2, 3])
└─────────────────┘ ── Avoid resizing arrays after initialization.
Avoid resizing arrays after initialization.
Rule: avoid-creating-empty-arrays-and-vectors. Severity: 8

AvoidCreatingEmptyArraysAndVectors.jl(55, 1):
insert!(w, 1, 5)
└──────────────┘ ── Avoid resizing arrays after initialization.
Avoid resizing arrays after initialization.
Rule: avoid-creating-empty-arrays-and-vectors. Severity: 8

AvoidCreatingEmptyArraysAndVectors.jl(56, 1):
deleteat!(w, 1)
└─────────────┘ ── Avoid resizing arrays after initialization.
Avoid resizing arrays after initialization.
Rule: avoid-creating-empty-arrays-and-vectors. Severity: 8

AvoidCreatingEmptyArraysAndVectors.jl(57, 1):
splice!(w, 4:3, 2)
└────────────────┘ ── Avoid resizing arrays after initialization.
Avoid resizing arrays after initialization.
Rule: avoid-creating-empty-arrays-and-vectors. Severity: 8

AvoidCreatingEmptyArraysAndVectors.jl(58, 1):
empty!(w)
└───────┘ ── Avoid resizing arrays after initialization.
Avoid resizing arrays after initialization.
Rule: avoid-creating-empty-arrays-and-vectors. Severity: 8

AvoidCreatingEmptyArraysAndVectors.jl(59, 1):
resize!(w, 100)
└─────────────┘ ── Avoid resizing arrays after initialization.
Avoid resizing arrays after initialization.
Rule: avoid-creating-empty-arrays-and-vectors. Severity: 8
