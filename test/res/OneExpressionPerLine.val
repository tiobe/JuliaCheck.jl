>> Processing file 'OneExpressionPerLine.jl'...

OneExpressionPerLine.jl(2, 30):
for bar::Int64 in range(1, 3); println("bar ", bar); if bar == 2 println("bar equals two"); end; end;
#                            ╙ ── Do not concatenate statements with a semicolon.
The number of expressions per line is limited to one.
Rule: one-expression-per-line. Severity: 7

OneExpressionPerLine.jl(2, 52):
for bar::Int64 in range(1, 3); println("bar ", bar); if bar == 2 println("bar equals two"); end; end;
#                                                  ╙ ── Do not concatenate statements with a semicolon.
The number of expressions per line is limited to one.
Rule: one-expression-per-line. Severity: 7

OneExpressionPerLine.jl(2, 91):
for bar::Int64 in range(1, 3); println("bar ", bar); if bar == 2 println("bar equals two"); end; end;
#                                                                                         ╙ ── Do not concatenate statements with a semicolon.
The number of expressions per line is limited to one.
Rule: one-expression-per-line. Severity: 7

OneExpressionPerLine.jl(2, 96):
for bar::Int64 in range(1, 3); println("bar ", bar); if bar == 2 println("bar equals two"); end; end;
#                                                                                              ╙ ── Do not concatenate statements with a semicolon.
The number of expressions per line is limited to one.
Rule: one-expression-per-line. Severity: 7

OneExpressionPerLine.jl(7, 10):
    x = 4; x + 5
#        ╙ ── Do not concatenate statements with a semicolon.
The number of expressions per line is limited to one.
Rule: one-expression-per-line. Severity: 7

OneExpressionPerLine.jl(8, 10):
    y = 6; z = 11;
#        ╙ ── Do not concatenate statements with a semicolon.
The number of expressions per line is limited to one.
Rule: one-expression-per-line. Severity: 7

OneExpressionPerLine.jl(13, 27):
    push!(clumsy_setup, 1); push!(clumsy_setup, 2)
#                         ╙ ── Do not concatenate statements with a semicolon.
The number of expressions per line is limited to one.
Rule: one-expression-per-line. Severity: 7

OneExpressionPerLine.jl(17, 10):
    x = 5; x + 5; x * 5;
#        ╙ ── Do not concatenate statements with a semicolon.
The number of expressions per line is limited to one.
Rule: one-expression-per-line. Severity: 7

OneExpressionPerLine.jl(17, 17):
    x = 5; x + 5; x * 5;
#               ╙ ── Do not concatenate statements with a semicolon.
The number of expressions per line is limited to one.
Rule: one-expression-per-line. Severity: 7

OneExpressionPerLine.jl(23, 14):
        b = 6; a = 4; # Violation expected here
#            ╙ ── Do not concatenate statements with a semicolon.
The number of expressions per line is limited to one.
Rule: one-expression-per-line. Severity: 7

OneExpressionPerLine.jl(29, 17):
            a= 7; b = 9; # Violation also expected here
#               ╙ ── Do not concatenate statements with a semicolon.
The number of expressions per line is limited to one.
Rule: one-expression-per-line. Severity: 7

OneExpressionPerLine.jl(35, 10):
    a = 1; b = 2; # Expect violation between 1 and b
#        ╙ ── Do not concatenate statements with a semicolon.
The number of expressions per line is limited to one.
Rule: one-expression-per-line. Severity: 7

OneExpressionPerLine.jl(37, 14):
        b = 1; c = 2; # Expect violation between 1 and c
#            ╙ ── Do not concatenate statements with a semicolon.
The number of expressions per line is limited to one.
Rule: one-expression-per-line. Severity: 7
