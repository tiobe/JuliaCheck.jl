
>> Processing file 'test/leading_and_trailing_digits.jl'...

test/leading_and_trailing_digits.jl(1, 29):
an_expression(x) = 1.5x^2 - .5x + 1
#                           └┘ ── Floating-point numbers should always have one digit before the decimal point and at least one after.
Leading and trailing digits.
Rule: asml-leading-and-trailing-digits. Severity: 3

test/leading_and_trailing_digits.jl(2, 1):
float_vec = Float64[.0, 1. ]
└───────┘ ── Avoid global variables wherever possible.
Avoid global variables when possible.
Rule: asml-avoid-global-variables. Severity: 3

test/leading_and_trailing_digits.jl(2, 1):
float_vec = Float64[.0, 1. ]
└───────┘ ── Variable float_vec should be written in UPPER_SNAKE_CASE.
Casing of globals.
Rule: asml-global-variables-upper-snake-case. Severity: 3

test/leading_and_trailing_digits.jl(2, 21):
float_vec = Float64[.0, 1. ]
#                   └┘ ── Floating-point numbers should always have one digit before the decimal point and at least one after.
Leading and trailing digits.
Rule: asml-leading-and-trailing-digits. Severity: 3

test/leading_and_trailing_digits.jl(2, 25):
float_vec = Float64[.0, 1. ]
#                       └┘ ── Floating-point numbers should always have one digit before the decimal point and at least one after.
Leading and trailing digits.
Rule: asml-leading-and-trailing-digits. Severity: 3

